# Local credential overrides — never commit real API keys
.claude/settings.local.json
.mcp.json

# Environment variable files
.env
.env.local
.env.*.local

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.mcp.json
